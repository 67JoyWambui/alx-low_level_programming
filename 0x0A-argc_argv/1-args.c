#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: the number of arguments
 * @argv: the number of strings in the arguments
 *
 * Return: 0 on success
 */
int main(int argc, char **argv)
{
	(void)argv;

	printf("%d\n", argc);
		return (0);
}
