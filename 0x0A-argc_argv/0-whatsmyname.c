#include <stdio.h>

/**
 * main - the entry point
 * @argc: the number of arguments
 *@argv: number of strings containing the arguments
 * Return : 0 on success
 */

int main(int argc, char *argv[])
{
	(void)argc;
	
	printf("%s\n" , argv[0]);
	return (0);
}
