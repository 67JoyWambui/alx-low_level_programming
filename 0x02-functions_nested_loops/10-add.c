#include "main.h"

/**
 * add - a function that adds two integers
 * @m: the first number
 * @n: the second number
 *
 * Return: the result
 */

int add(int m, int n)
{
	int result;

	result = m + n;

	return (result);
}
